# Internal design docs — kept out of this public repo
docs/specs/
docs/plans/

node_modules/
dist/
.astro/

# direnv: .envrc sources these; never commit one (cf. the #6 history scrub)
.env
.env.local
.direnv/

# nix devshell lock — not generated reproducibly in CI (pnpm-based); generate + force-add locally if committed reproducibility is wanted
flake.lock
